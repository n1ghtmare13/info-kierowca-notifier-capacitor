{
  "name": "info-kierowca-notifier-mobile",
  "version": "1.0.0",
  "description": "Mobile Capacitor App for info-kierowca exam slot notifications",
  "main": "www/index.html",
  "scripts": {
    "build": "npx cap copy",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync",
    "cap:open": "npx cap open android"
  },
  "dependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "@capacitor/local-notifications": "^6.0.0",
    "@capacitor/preferences": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0"
  }
}
